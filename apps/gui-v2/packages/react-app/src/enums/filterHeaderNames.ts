import { translate } from '../utils/translation';

enum FilterHeaderNames {
    All = 'All',
    Compliant = 'pcap.state.compliant',
    NotCompliant = 'pcap.state.not_compliant',
}

export default FilterHeaderNames;
