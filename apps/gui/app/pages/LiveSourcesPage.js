import React from 'react';
import SourcesList from '../containers/live/SourcesList';

const LiveSourcesPage = props => (
    <div className="row">
        <div className="col-xs-12">
                <SourcesList />
        </div>
    </div>
);

export default LiveSourcesPage;
